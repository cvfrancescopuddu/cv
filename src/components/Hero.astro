<section id="home" class="relative min-h-[85vh] flex items-center justify-center pt-16 overflow-hidden">
  
  <!-- Immagine di Sfondo con overlay sfumato -->
  <div class="absolute inset-0 z-0 opacity-0 hero-animate">
    <img 
      src="/img/pexels-tara-winstead-8386440.jpg" 
      alt="Hero background" 
      class="w-full h-full object-cover opacity-30"
    />
    <div class="absolute inset-0 bg-linear-to-t from-slate-950 via-slate-950/80 to-slate-950/20"></div>
  </div>

  <div class="relative z-10 max-w-4xl mx-auto px-4 text-center">
    
    <!-- 1. Badge (Appare per primo) -->
    <div class="opacity-0 hero-animate delay-1">
      <span class="inline-block px-4 py-1.5 mb-6 text-xs font-semibold tracking-wider text-teal-400 uppercase bg-teal-950/80 border border-teal-800/50 rounded-full">
        Developer Indipendente
      </span>
    </div>

    <!-- 2. Titolo Principale (Appare per secondo) -->
    <h1 class="text-4xl sm:text-6xl font-extrabold text-white tracking-tight mb-6 opacity-0 hero-animate delay-2">
      Developing Your Future
    </h1>

    <!-- 3. Descrizione (Appare per terza) -->
    <p class="text-lg sm:text-xl text-slate-300 max-w-2xl mx-auto mb-8 font-light leading-relaxed opacity-0 hero-animate delay-3">
      Sviluppo soluzioni software pulite, efficienti e basate sui dati, unendo la passione per l'informatica all'analisi continua.
    </p>

    <!-- 4. Pulsanti CTA (Appaiono per ultimi) -->
    <div class="flex flex-wrap justify-center gap-4 opacity-0 hero-animate delay-4">
      <a href="#contatti" class="px-6 py-3 rounded-xl font-semibold bg-teal-500 text-slate-950 hover:bg-teal-400 transition-all shadow-lg shadow-teal-500/20 hover:scale-[1.02]">
        Contattami
      </a>
      <a href="#presentazione" class="px-6 py-3 rounded-xl font-semibold bg-slate-900 text-white hover:bg-slate-800 border border-slate-800 transition-all hover:scale-[1.02]">
        Scopri il mio percorso
      </a>
    </div>

  </div>
</section>